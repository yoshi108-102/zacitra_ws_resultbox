[project]
name = "literature-portal-api"
version = "0.1.0"
description = "Lambda handlers for PDF upload APIs"
requires-python = ">=3.11"
dependencies = [
  "boto3>=1.34,<2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
